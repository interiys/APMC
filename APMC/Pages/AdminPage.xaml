<Page x:Class="APMC.Pages.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:APMC.Pages"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Панель администратора"
      Background="#F5F5F5"
      IsVisibleChanged="Window_IsVisibleChanged">

    <Grid Margin="-102,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" 
                Background="#2196F3" 
                Padding="20"
                Height="80">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                          Text="Панель администратора - Управление пользователями"
                          FontSize="24" 
                          FontWeight="Bold" 
                          Foreground="White"
                          HorizontalAlignment="Left" Margin="127,0,0,0" Width="679"/>

                <TextBlock x:Name="TBHello" 
                          Grid.Column="1"
                          Text=""
                          FontSize="14" 
                          Foreground="White"
                          HorizontalAlignment="Right"
                          VerticalAlignment="Center"
                          Margin="0,0,10,0"/>
            </Grid>
        </Border>

        <!-- Основной контент -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>

            <!-- Таблица пользователей -->
            <Border Grid.Column="0" 
                   Background="White"
                   CornerRadius="10"
                   Padding="10"
                   Effect="{StaticResource ShadowMedium}" Margin="-304,0,0,-126">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                              Text="Список пользователей"
                              FontSize="18"
                              FontWeight="Bold"
                              Foreground="#2196F3"
                              Margin="0,0,0,10"
                              HorizontalAlignment="Center"/>

                    <ScrollViewer Grid.Row="1" 
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto">
                        <DataGrid x:Name="DGridUsers"
                                  SelectionChanged="DGridUsers_SelectionChanged"
                                  AutoGenerateColumns="False"
                                  SelectionMode="Single"
                                  SelectionUnit="FullRow"
                                  HeadersVisibility="Column"
                                  BorderBrush="#E0E0E0"
                                  BorderThickness="1"
                                  RowBackground="White"
                                  AlternatingRowBackground="#F8F8F8"
                                  GridLinesVisibility="Horizontal"
                                  FontSize="14"
                                  RowHeight="35" Width="948" Height="529">
                            <DataGrid.Resources>
                                <!-- Стиль для заголовков колонок -->
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#2196F3"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Padding" Value="10,5"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                </Style>
                            </DataGrid.Resources>

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Фамилия" 
                                                   Binding="{Binding LastName}"
                                                   Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="10,5"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="Имя" 
                                                   Binding="{Binding FirstName}"
                                                   Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="10,5"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="Отчество" 
                                                   Binding="{Binding Patronymic}"
                                                   Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="10,5"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="Роль" 
                                                   Binding="{Binding Role.RoleName}"
                                                   Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="10,5"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="Цех" 
                                                   Binding="{Binding Department.DepartmentName}"
                                                   Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="10,5"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="Статус" 
                                                   Binding="{Binding UserStatus1.UserStatusName}"
                                                   Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="10,5"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTemplateColumn Header="Действия" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Редактировать"
                                                    Click="ButtonEditUser_Click"
                                                    Width="110"
                                                    Height="30"
                                                    FontSize="12"
                                                    Background="#FF9800"
                                                    Foreground="White"
                                                    FontWeight="SemiBold"
                                                    BorderThickness="0"
                                                    Cursor="Hand">
                                                <Button.Effect>
                                                    <DropShadowEffect BlurRadius="3" ShadowDepth="1" Opacity="0.3"/>
                                                </Button.Effect>
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#F57C00"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Панель управления -->
            <Border Grid.Column="1" 
                   Margin="20,0,0,0"
                   Background="White"
                   CornerRadius="10"
                   Padding="15"
                   Effect="{StaticResource ShadowMedium}">
                <StackPanel>
                    <TextBlock Text="Управление"
                              FontSize="16"
                              FontWeight="Bold"
                              Foreground="#2196F3"
                              Margin="0,0,0,20"
                              HorizontalAlignment="Center"/>

                    <Button Content="Добавить" 
                           Click="ButtonAddUser_Click"
                           Height="45"
                           FontSize="14"
                           Background="#4CAF50"
                           Foreground="White"
                           FontWeight="Bold"
                           Margin="0,0,0,15"
                           Cursor="Hand">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.3"/>
                        </Button.Effect>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#45a049"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="Удаление" 
                           Click="ButtonDeleteUser_Click"
                           Height="45"
                           FontSize="14"
                           Background="#9C27B0"
                           Foreground="White"
                           FontWeight="Bold"
                           Margin="0,0,0,15"
                           Cursor="Hand">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.3"/>
                        </Button.Effect>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#7B1FA2"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="Логгер" 
                           Click="ButtonLogs_Click"
                           Height="45"
                           FontSize="14"
                           Background="#9C27B0"
                           Foreground="White"
                           FontWeight="Bold"
                           Margin="0,0,0,15"
                           Cursor="Hand">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.3"/>
                        </Button.Effect>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#7B1FA2"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- Статистика -->
                    <Border Background="#E3F2FD"
                           CornerRadius="5"
                           Padding="10"
                           Margin="0,20,0,0">
                        <StackPanel>
                            <TextBlock Text="Статистика"
                                      FontSize="14"
                                      FontWeight="Bold"
                                      Foreground="#1976D2"
                                      Margin="0,0,0,10"
                                      HorizontalAlignment="Center"/>

                            <StackPanel Orientation="Horizontal"
                                       Margin="0,0,0,5">
                                <TextBlock Text="Всего пользователей:"
                                          FontSize="12"
                                          Foreground="#333333"/>
                                <TextBlock x:Name="TBTotalUsers"
                                          Text="0"
                                          FontSize="12"
                                          FontWeight="Bold"
                                          Foreground="#1976D2"
                                          Margin="5,0,0,0"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal"
                                       Margin="0,5,0,0">
                                <TextBlock Text="Активные:"
                                          FontSize="12"
                                          Foreground="#333333"/>
                                <TextBlock x:Name="TBActiveUsers"
                                          Text="0"
                                          FontSize="12"
                                          FontWeight="Bold"
                                          Foreground="#4CAF50"
                                          Margin="5,0,0,0"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal"
                                       Margin="0,5,0,0">
                                <TextBlock Text="Администраторы:"
                                          FontSize="12"
                                          Foreground="#333333"/>
                                <TextBlock x:Name="TBAdminUsers"
                                          Text="0"
                                          FontSize="12"
                                          FontWeight="Bold"
                                          Foreground="#FF9800"
                                          Margin="5,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Нижняя панель -->
        <Border Grid.Row="2" 
               Background="#E8EAF6" 
               Padding="15"
               Height="70">
            <StackPanel Orientation="Horizontal" 
                       HorizontalAlignment="Right">
                <Button Content="Обновить" 
                       Click="ButtonRefresh_Click"
                       Width="120" Height="40"
                       FontSize="14"
                       Background="#2196F3"
                       Foreground="White"
                       FontWeight="Bold"
                       Margin="0,0,10,0"
                       Cursor="Hand">
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.3"/>
                    </Button.Effect>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1976D2"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Выйти" 
                       Click="ButtonExit_Click"
                       Width="120" Height="40"
                       FontSize="14"
                       Background="#757575"
                       Foreground="White"
                       FontWeight="Bold"
                       Cursor="Hand">
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.3"/>
                    </Button.Effect>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#616161"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Page>