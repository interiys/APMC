<Page x:Class="APMC.Pages.FasPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:APMC.Pages"
      mc:Ignorable="d"
      d:DesignHeight="550" d:DesignWidth="900"
      Title="Учет фасовки"
      Background="#F5F5F5"
      IsVisibleChanged="Window_IsVisibleChanged">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" 
                Background="#2196F3" 
                Padding="20"
                Height="80">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                          Text="Учет фасовки продукции"
                          FontSize="24" 
                          FontWeight="Bold" 
                          Foreground="White"
                          HorizontalAlignment="Left"
                          VerticalAlignment="Center"/>

                <TextBlock x:Name="TBHello" 
                          Grid.Column="1"
                          Text=""
                          FontSize="14" 
                          Foreground="White"
                          HorizontalAlignment="Right"
                          VerticalAlignment="Center"
                          Margin="0,0,10,0"/>
            </Grid>
        </Border>

        <!-- Основной контент -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>

            <!-- Таблица с прокруткой -->
            <Border Grid.Column="0" 
                   Background="White"
                   CornerRadius="10"
                   Padding="10"
                   Effect="{StaticResource ShadowMedium}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                              Text="Записи фасовки"
                              FontSize="18"
                              FontWeight="Bold"
                              Foreground="#2196F3"
                              Margin="0,0,0,10"
                              HorizontalAlignment="Center"/>

                    <ScrollViewer Grid.Row="1" 
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto">
                        <DataGrid x:Name="DGridFasTable"
                                  SelectionChanged="DGridFasTables_SelectionChanged"
                                  AutoGenerateColumns="False"
                                  SelectionMode="Single"
                                  SelectionUnit="FullRow"
                                  HeadersVisibility="Column"
                                  BorderBrush="#E0E0E0"
                                  BorderThickness="1"
                                  RowBackground="White"
                                  AlternatingRowBackground="#F8F8F8"
                                  GridLinesVisibility="Horizontal"
                                  FontSize="14"
                                  RowHeight="35">
                            <DataGrid.Resources>
                                <!-- Стиль для заголовков колонок -->
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#2196F3"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Padding" Value="10,5"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                </Style>
                            </DataGrid.Resources>

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Дата производства"
                                                   Binding="{Binding DateProduction}"
                                                   Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="10,5"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="Цех"
                                                   Binding="{Binding DepartmentID}"
                                                   Width="80">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="10,5"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="Ед. измерения" 
                                                   Binding="{Binding UnitOfMeasuresID}"
                                                   Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="10,5"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="Тип продукции" 
                                                   Binding="{Binding TypeID}"
                                                   Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="10,5"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="Количество" 
                                                   Binding="{Binding Quantity}"
                                                   Width="90">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="10,5"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="Статус" 
                                                   Binding="{Binding StatuseID}"
                                                   Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="10,5"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTemplateColumn Header="Действия" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Редактировать"
                                                    Click="ButtonEditFas_Click"
                                                    Width="110"
                                                    Height="30"
                                                    FontSize="12"
                                                    Background="#FF9800"
                                                    Foreground="White"
                                                    FontWeight="SemiBold"
                                                    BorderThickness="0"
                                                    Cursor="Hand">
                                                <Button.Effect>
                                                    <DropShadowEffect BlurRadius="3" ShadowDepth="1" Opacity="0.3"/>
                                                </Button.Effect>
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#F57C00"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Панель управления -->
            <Border Grid.Column="1" 
                   Margin="20,0,0,0"
                   Background="White"
                   CornerRadius="10"
                   Padding="15"
                   Effect="{StaticResource ShadowMedium}">
                <StackPanel>
                    <TextBlock Text="Управление"
                              FontSize="16"
                              FontWeight="Bold"
                              Foreground="#2196F3"
                              Margin="0,0,0,20"
                              HorizontalAlignment="Center"/>

                    <Button Content="Добавить" 
                           Click="ButtonAddFas_Click"
                           Height="45"
                           FontSize="14"
                           Background="#4CAF50"
                           Foreground="White"
                           FontWeight="Bold"
                           Margin="0,0,0,15"
                           Cursor="Hand">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.3"/>
                        </Button.Effect>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#45a049"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="Смена" 
                           Click="ButtonSmenFas_Click"
                           Height="45"
                           FontSize="14"
                           Background="#2196F3"
                           Foreground="White"
                           FontWeight="Bold"
                           Margin="0,0,0,15"
                           Cursor="Hand">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.3"/>
                        </Button.Effect>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1976D2"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- Статистика -->
                    <Border Background="#E3F2FD"
                           CornerRadius="5"
                           Padding="10"
                           Margin="0,20,0,0">
                        <StackPanel>
                            <TextBlock Text="Статистика"
                                      FontSize="14"
                                      FontWeight="Bold"
                                      Foreground="#1976D2"
                                      Margin="0,0,0,10"
                                      HorizontalAlignment="Center"/>

                            <StackPanel Orientation="Horizontal"
                                       Margin="0,0,0,5">
                                <TextBlock Text="Всего записей:"
                                          FontSize="12"
                                          Foreground="#333333"/>
                                <TextBlock x:Name="TBTotalRecords"
                                          Text="0"
                                          FontSize="12"
                                          FontWeight="Bold"
                                          Foreground="#1976D2"
                                          Margin="5,0,0,0"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal"
                                       Margin="0,5,0,0">
                                <TextBlock Text="Сегодня:"
                                          FontSize="12"
                                          Foreground="#333333"/>
                                <TextBlock x:Name="TBTodayRecords"
                                          Text="0"
                                          FontSize="12"
                                          FontWeight="Bold"
                                          Foreground="#1976D2"
                                          Margin="5,0,0,0"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal"
                                       Margin="0,5,0,0">
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Нижняя панель -->
        <Border Grid.Row="2" 
               Background="#E8EAF6" 
               Padding="15">
            <StackPanel Orientation="Horizontal" 
                       HorizontalAlignment="Right">
                <Button Content="Выйти" 
                       Click="ButtonExit_Click"
                       Width="120" Height="40"
                       FontSize="14"
                       Background="#757575"
                       Foreground="White"
                       FontWeight="Bold"
                       Margin="0,0,10,0"
                       Cursor="Hand">
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.3"/>
                    </Button.Effect>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#616161"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Обновить" 
                       Click="ButtonRefresh_Click"
                       Width="120" Height="40"
                       FontSize="14"
                       Background="#9C27B0"
                       Foreground="White"
                       FontWeight="Bold"
                       Cursor="Hand">
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.3"/>
                    </Button.Effect>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#7B1FA2"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Page>